# Packages actually used by this analysis
torch>=2.0            # CNN training (requires a CUDA GPU)
torchvision>=0.15     # ResNet-50 with ImageNet weights
numpy>=1.24
pandas>=2.0
scipy>=1.10           # statistical tests, Gaussian prefilter for resizing
scikit-learn>=1.3     # StratifiedGroupKFold, metrics, logistic regression
matplotlib>=3.8       # figures (Arial must be installed)
pillow>=10.0
openpyxl>=3.1         # supplementary table workbook
python-docx>=1.1      # manuscript typesetting (not needed for the analysis)
